<% if post.comments_counter == 0 %>
  <div class='comments'>
    <p>No comments yet!</p>
  </div>
<% end %>

<% if post.comments_counter > 0 %>
  <ul class='comments'>
    <% comments.each do |comment| %>
      <li class='comment'>
        <p><%= comment.author.id == 2 ? 'Me' : comment.author.name %></p>
        <p class='comment-box border1'><%= comment.text %></p>
      </li>
    <% end %>
  </ul>
<% end %>