<section>
  <h1 class='users_title'>Users</h1>

  <ul>
    <% if @users.empty? %>
      <p class='users-empty-message'>No users yet!</p>
    <% else %>
      <% @users.each do |user| %>
        <%= render 'shared/user_card', user: user %>
      <% end %>
    <% end %>
  </ul>
</section>